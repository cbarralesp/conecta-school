CREATE EXTENSION IF NOT EXISTS pgcrypto;

DROP TABLE IF EXISTS "USUARIOS";
DROP TABLE IF EXISTS "PERSONAS";

CREATE TABLE "PERSONAS" (
    "ID" BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    "RUN" VARCHAR(20) NOT NULL UNIQUE,
    "NOMBRES" VARCHAR(120) NOT NULL,
    "APELLIDOS" VARCHAR(120) NOT NULL,
    "CORREO_ELECTRONICO" VARCHAR(180) NOT NULL UNIQUE,
    "DIRECCION" VARCHAR(250),
    "TELEFONO" VARCHAR(30)
);

CREATE TABLE "USUARIOS" (
    "ID" BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    "PERSONA_ID" BIGINT NOT NULL UNIQUE,
    "USUARIO" VARCHAR(80) NOT NULL UNIQUE,
    "CLAVE" VARCHAR(255) NOT NULL,
    "ACTIVO" BOOLEAN NOT NULL DEFAULT TRUE,
    CONSTRAINT "FK_USUARIOS_PERSONAS" FOREIGN KEY ("PERSONA_ID") REFERENCES "PERSONAS" ("ID")
);

INSERT INTO "PERSONAS" ("RUN", "NOMBRES", "APELLIDOS", "CORREO_ELECTRONICO", "DIRECCION", "TELEFONO")
VALUES
    ('11111111-1', 'Juan', 'Perez Soto', 'juan.perez@escuela.cl', 'Av. Central 123', '+56911111111'),
    ('22222222-2', 'Maria', 'Gonzalez Rojas', 'maria.gonzalez@escuela.cl', 'Calle Los Aromos 456', '+56922222222');

INSERT INTO "USUARIOS" ("PERSONA_ID", "USUARIO", "CLAVE", "ACTIVO")
VALUES
    (1, 'jperez', crypt('jperez123', gen_salt('bf', 12)), TRUE),
    (2, 'mgonzalez', crypt('mgonzalez123', gen_salt('bf', 12)), TRUE);
